{
  "name": "app-errorlist",
  "version": "0.3.0",
  "description": "Devebot error pool layerware",
  "homepage": "https://github.com/apporo/app-exception.git",
  "author": {
    "name": "apporo",
    "email": "apporo@devebot.com",
    "url": "https://github.com/apporo"
  },
  "main": "index.js",
  "directories": {
    "lib": "lib/"
  },
  "files": [
    "bin/",
    "config/",
    "data/",
    "lib/"
  ],
  "scripts": {
    "build": "babel ./src -d ./lib",
    "clean": "rm -rf ./lib",
    "prepublish": "npm run clean && NODE_ENV=production npm run build",
    "pretest": "npm run clean && NODE_ENV=test npm run build",
    "test": "cucumber-js ./test/**/features/*.feature",
    "verify": "npm run prepublish && cucumber-js ./test/**/features/*.feature"
  },
  "keywords": [
    "devebot",
    "devebot-plugin"
  ],
  "peerDependencies": {
    "devebot": "^0.3.0"
  },
  "dependencies": {
    "devebot": "^0.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-minify-mangle-names": "^0.3.0",
    "babel-preset-env": "^1.6.1"
  }
}
